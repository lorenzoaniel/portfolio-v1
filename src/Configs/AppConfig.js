const AppConfig = () => {
    return {
        about: {
            gridArea: "main"
        },
        projects: {
            gridArea: "main"
        },
        contact: {
            gridArea: "main"
        },
        navbar: {
            gridArea: "navbar"
        }
    }
}

export default AppConfig